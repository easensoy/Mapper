# Interlock generation policy. The rules themselves come from Control.xml via InterlockPlanner;
# this file is generation-side policy only.

# FLOOR for the generated rule array. The deployed size is whatever the plan needs, never less than
# this, so a model with more interlocks than the floor grows the array instead of losing rows.
ruleArraySize: 10
