{
  "_comment": "StateSync RUNTIME config. Only non-Control.xml settings live here. gen_sync_map.py merges this with the components discovered from Control.xml into sync-map.generated.json (the runtime map the bridge loads). Edit this file for site settings; never hand-list components.",
  "broker": { "host": "192.168.1.50", "port": 1883 },
  "unsPrefix": "uns/wmg/smc_rig/v1",
  "unsQos": 1,
  "vueone": { "enabled": true, "host": "127.0.0.1", "port": 51000, "clientId": "VC" }
}
